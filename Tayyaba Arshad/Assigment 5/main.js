"use strict";
// store a famous person name in a variable 
let famPerson = "shakespear";
let message = `${famPerson} once said, "There is nothing either good or bad, but thinking makes it so."`;
// print a message
console.log(message);
